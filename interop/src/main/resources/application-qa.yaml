# ============================================================================
# Test Profile Configuration
# ============================================================================

# ============================================================================
# External Configuration Import
# ============================================================================
spring:
  config:
    activate:
      on-profile: qa

# ============================================================================
# URLs Configuration
# Base URLs and routes for test environment
# ============================================================================
interop:
  auth:
    aws:
      region: eu-south-1
    bearer:
      well-known-url: https://qa.interop.pagopa.it/.well-known/jwks.json
      issuer: qa.interop.pagopa.it
      audience: qa.interop.pagopa.it/ui
      duration-sec: 14400
    client-assertion:
      audience: auth.qa.interop.pagopa.it/client-assertion
      grant_type: client_credentials
      assertion_type: urn:ietf:params:oauth:client-assertion-type:jwt-bearer
  api:
    base-url:
      bff: https://selfcare.qa.interop.pagopa.it/1.0/backend-for-frontend
  web:
    base-url: https://selfcare.qa.interop.pagopa.it/ui/it/t

url:
  notifiche:
    cittadino:
      base: https://cittadini.test.notifichedigitali.it
    persona-giuridica:
      base: https://personegiuridiche.test.notifichedigitali.it
    mittente:
      base: https://selfcare.test.notifichedigitali.it
      dashboard: ${url.notifiche.mittente.base}/dashboard
      new-notification: ${url.notifiche.mittente.dashboard}/nuova-notifica
      api-keys: ${url.notifiche.mittente.base}/api-keys
      new-api-key: ${url.notifiche.mittente.api-keys}/nuova-api-key
      app-status: ${url.notifiche.mittente.base}/app-status
      statistics: ${url.notifiche.mittente.base}/statistiche
    supporto:
      base: https://uat.selfcare.pagopa.it