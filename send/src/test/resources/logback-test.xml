<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_DIR" value="target/logs"/>
    <property name="LOG_PATTERN_WITH_TEST"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{scenario}] [%thread] %-5level %logger{36} : %msg%n"/>
    <property name="LOG_PATTERN_SIMPLE"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} : %msg%n"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN_WITH_TEST}</pattern>
        </encoder>
    </appender>

    <!-- File Appender per il framework -->
    <appender name="FRAMEWORK_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/framework.log</file>
        <encoder>
            <pattern>${LOG_PATTERN_SIMPLE}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/framework.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- File Appender per i test Cucumber (CON TEST CONTEXT) -->
    <appender name="TEST_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/e2e-tests.log</file>
        <encoder>
            <pattern>${LOG_PATTERN_WITH_TEST}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/e2e-tests.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Appender separato per log di scenario -->
    <appender name="SCENARIO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/scenarios.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{scenario}] %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/scenarios.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Logger per il tracking degli scenari -->
    <logger name="SCENARIO_TRACKING" level="INFO" additivity="false">
        <appender-ref ref="SCENARIO_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Logger del framework -->
    <logger name="FRAMEWORK" level="INFO" additivity="false">
        <appender-ref ref="FRAMEWORK_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Logger per Cucumber e test -->
    <logger name="it.pn.frontend.e2e" level="INFO" additivity="false">
        <appender-ref ref="TEST_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Logger Spring/Selenium ridotti -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.openqa.selenium" level="WARN"/>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="TEST_FILE"/>
    </root>
</configuration>